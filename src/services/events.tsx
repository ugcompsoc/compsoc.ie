
// Makes a request to the API to get all events and saves all events locally for further tweaking (seperating between old and new)
const getAllEvents = (() => {
    
})

// Returns all events that have an end time after the current time
const getPastEvents = ((lowerBound: number, upperBound: number) => {

})

// Returns all events that have an end time before the current time
const getUpcomingEvents = ((owerBound: number, upperBound: number) => {

})

// Returns all events seperated into two arrays, past and upcoming
export const getEvents = (() => {

})